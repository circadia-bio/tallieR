% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{completion_summary}
\alias{completion_summary}
\title{Completion summary}
\usage{
completion_summary(obj, wide = FALSE, include_date = TRUE, include_meta = TRUE)
}
\arguments{
\item{obj}{A \code{tallier_export} or \code{tallier_study} object.}

\item{wide}{Logical. If \code{FALSE} (default), returns a long data frame with
one row per participant × questionnaire and a \code{completed} logical column.
If \code{TRUE}, returns a wide data frame with one row per participant and one
logical column per questionnaire.}

\item{include_date}{Logical. If \code{TRUE} (default), adds a \code{completed_at}
column in long format showing the timestamp of the most recent
administration. Ignored when \code{wide = TRUE}.}

\item{include_meta}{Logical. If \code{TRUE} (default), participant metadata
columns are prepended.}
}
\value{
In long format: a \code{data.frame} with columns: participant metadata
(optional), \code{questionnaire_id}, \code{completed} (logical), and optionally
\code{completed_at} (character timestamp of most recent administration).
In wide format: a \code{data.frame} with one row per participant and one
logical column per questionnaire.
}
\description{
Returns a data frame showing which questionnaires each participant has
completed. Useful for monitoring data collection progress in longitudinal
or multi-site studies.
}
\details{
When a participant has completed a questionnaire more than once, it is
counted as complete and the most recent \code{completed_at} timestamp is
reported (when \code{include_date = TRUE}).
}
\examples{
\dontrun{
study <- read_scoreme_dir("exports/")

# Long format: one row per participant x questionnaire
completion_summary(study)

# Wide format: one row per participant
completion_summary(study, wide = TRUE)

# Without timestamps
completion_summary(study, include_date = FALSE)
}

}
\seealso{
\code{\link[=scores_wide]{scores_wide()}}, \code{\link[=scores_long]{scores_long()}}
}
