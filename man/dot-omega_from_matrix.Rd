% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{.omega_from_matrix}
\alias{.omega_from_matrix}
\title{Compute McDonald's omega (total) from a numeric item matrix}
\usage{
.omega_from_matrix(mat)
}
\arguments{
\item{mat}{A numeric matrix (participants x items), complete cases only.}
}
\value{
Named list with \code{omega}, \code{n_items}, \code{n_obs}, \code{note}.
}
\description{
Uses a single-factor EFA via \code{stats::factanal()} to extract factor loadings,
then applies the omega_t formula:
\code{omega = (sum(lambda))^2 / ((sum(lambda))^2 + sum(1 - lambda^2))}
where \code{lambda} are the standardised factor loadings.
}
\keyword{internal}
