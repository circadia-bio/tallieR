% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questionnaires.R
\name{interpret_all}
\alias{interpret_all}
\title{Interpret all questionnaire scores in an export}
\usage{
interpret_all(obj, include_meta = TRUE, instruments = NULL)
}
\arguments{
\item{obj}{A \code{tallier_export} or \code{tallier_study} object.}

\item{include_meta}{Logical. If \code{TRUE} (default), participant metadata
columns are prepended (same columns as \code{\link[=scores_long]{scores_long()}}).}

\item{instruments}{An optional named list of additional registry entries
from \code{\link[=load_instrument]{load_instrument()}} or \code{\link[=load_instrument_dir]{load_instrument_dir()}}.}
}
\value{
A \code{data.frame} with columns: participant metadata (optional),
\code{questionnaire_id}, \code{completed_at}, \code{score}, \code{label}, \code{color},
\code{description}.
}
\description{
Returns a long data frame with one row per participant x questionnaire x
administration, augmented with clinical interpretation columns (\code{label},
\code{color}, \code{description}). Mirrors the shape of \code{\link[=scores_long]{scores_long()}} so the two
can be joined by \code{participant_id} + \code{questionnaire_id} + \code{completed_at}.
}
\details{
Scores that cannot be interpreted (unknown instrument, \code{NA} score, or
composite score with no matching band) return \code{NA} in all three
interpretation columns rather than an error, so the rest of the study
data is unaffected.
}
\examples{
\dontrun{
study <- read_scoreme_dir("exports/")
interps <- interpret_all(study)

# Join with scores_long() if you need both
scores <- scores_long(study)
full   <- merge(scores, interps[
  c("participant_id", "questionnaire_id", "completed_at",
    "label", "color", "description")
], by = c("participant_id", "questionnaire_id", "completed_at"),
  all.x = TRUE)
}

}
\seealso{
\code{\link[=interpret_score]{interpret_score()}}, \code{\link[=scores_long]{scores_long()}}
}
