% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{cronbach_alpha}
\alias{cronbach_alpha}
\title{Cronbach's alpha for one or more questionnaires}
\usage{
cronbach_alpha(obj, questionnaires = NULL, conf_level = 0.95, min_items = 2L)
}
\arguments{
\item{obj}{A \code{tallier_export} or \code{tallier_study} object, or a data frame
as returned by \code{\link[=items_long]{items_long()}} (must contain columns \code{participant_id},
\code{questionnaire_id}, \code{item_id}, \code{completed_at}, \code{response}).}

\item{questionnaires}{Character vector of questionnaire IDs to include.
Defaults to all questionnaires present in \code{obj}.}

\item{conf_level}{Numeric. Confidence level for the CI (default \code{0.95}).
Uses the exact F-distribution method of Feldt et al. (1987).}

\item{min_items}{Integer. Minimum number of numeric items required to
attempt estimation (default \code{2}).}
}
\value{
A \code{data.frame} with one row per questionnaire and columns:
\describe{
\item{questionnaire_id}{Questionnaire identifier.}
\item{alpha}{Cronbach's alpha (standardised scale: −∞ to 1).}
\item{ci_lower}{Lower bound of the \code{conf_level} CI.}
\item{ci_upper}{Upper bound of the \code{conf_level} CI.}
\item{n_items}{Number of numeric items used.}
\item{n_obs}{Number of complete observations (participants).}
\item{note}{\code{NA} on success, or a short message explaining why
estimation failed.}
}
}
\description{
Computes Cronbach's alpha (a measure of internal consistency) for each
questionnaire present in a \code{tallier_export} or \code{tallier_study} object.
Item-level responses are extracted via \code{\link[=items_long]{items_long()}}, coerced to numeric,
and a participant × item matrix is constructed per questionnaire.
}
\details{
Non-numeric items (e.g. MCTQ clock times, STOP-BANG yes/no) are silently
dropped before estimation. Questionnaires with fewer than 2 numeric items
or fewer than 2 complete observations return \code{NA} with an explanatory note.
}
\examples{
\dontrun{
study <- read_scoreme_dir("exports/")

# All questionnaires
cronbach_alpha(study)

# Specific subset
cronbach_alpha(study, questionnaires = c("ess", "isi", "phq9"))

# From an items_long() data frame (e.g. already filtered)
items <- items_long(study)
cronbach_alpha(items)
}

}
\references{
Cronbach, L. J. (1951). Coefficient alpha and the internal structure of
tests. \emph{Psychometrika}, 16(3), 297–334.
\doi{10.1007/BF02310555}

Feldt, L. S., Woodruff, D. J., & Salih, F. A. (1987). Statistical
inference for coefficient alpha. \emph{Applied Psychological Measurement},
11(1), 93–103. \doi{10.1177/014662168701100107}
}
