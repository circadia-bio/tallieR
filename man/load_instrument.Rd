% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom_instruments.R
\name{load_instrument}
\alias{load_instrument}
\title{Load a custom instrument from a ScoreMe JSON spec}
\usage{
load_instrument(path)
}
\arguments{
\item{path}{Path to a \code{.json} file containing a ScoreMe instrument spec.
See \code{vignette("custom-instruments")} or
\code{system.file("extdata", "example_instrument.json", package = "tallieR")}
for the expected schema.}
}
\value{
A named list (a single tallieR registry), where the name is the
instrument \code{id} from the spec. Invisibly also carries the original spec
in \verb{$<id>$spec}.
}
\description{
Reads a \code{.json} file produced for (or exported from) the ScoreMe app and
compiles it into a tallieR registry entry. The result can be passed to
\code{\link[=score_questionnaire]{score_questionnaire()}} via its \code{instruments} argument, combined with other
custom entries using \code{\link[=c]{c()}}, or stored in a project-level list for reuse.
}
\details{
\strong{Supported scoring types:} \code{"sum"}, \code{"weighted_sum"}, \code{"mean"}. For
instruments with \code{"composite"} scoring (e.g. PSQI-style multi-component
algorithms) the returned entry will score as \code{NA} and a warning is emitted
each time scoring is attempted. You can override the compiled \code{score}
function by assigning a custom one after loading (see examples).
}
\examples{
\dontrun{
my_instr <- load_instrument("path/to/fss.json")

# Score a single result
score_questionnaire("fss", answers, instruments = my_instr)

# Combine with another custom instrument
registry <- c(load_instrument("fss.json"), load_instrument("vas_pain.json"))

# Override scoring for a composite instrument
my_psqi <- load_instrument("psqi_custom.json")
my_psqi$psqi_custom$score <- function(answers) { ... }
}

}
\seealso{
\code{\link[=load_instrument_dir]{load_instrument_dir()}}, \code{\link[=score_questionnaire]{score_questionnaire()}},
\code{\link[=available_instruments]{available_instruments()}}
}
